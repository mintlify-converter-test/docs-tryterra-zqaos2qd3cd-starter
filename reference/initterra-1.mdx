---
title: "initTerra"
description: "Instantiates the TerraManager class from either platform"
---
```dart
static Future<SuccessMessage?> initTerra(
      String devID,
      String referenceID) async

```

`String devId` ➡ The dev-id provided to you when signing up with Terra [Developer Dashboard](/docs/developer-dashboard)
  
`String referenceID` ➡ A random string used to characterise a user on your server

**returns**  
`Future<SuccessMessage>`



